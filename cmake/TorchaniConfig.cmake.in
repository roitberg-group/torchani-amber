@PACKAGE_INIT@
include(CMakeFindDependencyMacro)
# package init is included for consistency but the macros are unused here, only
# imported libraries are provided

# Include the imported targets this cmake project defines into a downstream
# project that uses cmake (such as Amber) only if the targets have not been
# already provided

# targets are namespaced inside the ${PACKAGE_NAME}Targets.cmake file

# Momentarily change CMAKE prefix path to find the necessary dependencies for
# the imported targets

set(ORIGINAL_CMAKE_PREFIX_PATH ${CMAKE_PREFIX_PATH})
set(ORIGINAL_CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH})
set(CMAKE_PREFIX_PATH @CMAKE_PREFIX_PATH@)
set(CMAKE_MODULE_PATH @CMAKE_MODULE_PATH@)

set(CUDNN_LIBRARY_PATH @CUDNN_LIBRARY_PATH@)
set(CUDNN_INCLUDE_PATH @CUDNN_INCLUDE_PATH@)

find_dependency(Torch QUIET REQUIRED)
include(@PACKAGE_PROJECT_TARGETS_PATH@)

set(CMAKE_PREFIX_PATH ${ORIGINAL_CMAKE_PREFIX_PATH})
set(CMAKE_MODULE_PATH ${ORIGINAL_CMAKE_MODULE_PATH})

check_required_components(Torchani)
